{
  "name": "@voxpupuli/enterprise-console-marketing",
  "version": "0.0.0",
  "private": true,
  "description": "Development-only tooling for the marketing site. The site itself takes voxblocks from frontend/'s lockfile, not from here, so the two cannot drift apart.",
  "devDependencies": {
    "axe-core": "^4.10.2"
  }
}
