## 1. cmd/node-agent-client: Windows service integration

- [x] 1.1 Refactor `main.go` so its bootstrap logic (config load, client construction, run-until-cancelled) lives in a shared `run(ctx context.Context) error`, called by both the plain path and the new Windows-service path - verify `go build ./cmd/node-agent-client` still succeeds on the host platform and existing behavior is unchanged (no functional change yet, pure refactor) - added `service_other.go` (`!windows` stub returning `false`) now too, since `main.go` needed `runAsWindowsService` to exist to compile; `go build ./...` and `go vet ./cmd/node-agent-client/...` both clean
- [x] 1.2 Add `golang.org/x/sys/windows/svc` as a dependency; create `service_windows.go` (`//go:build windows`) implementing a minimal `svc.Handler` that calls `run` with a context cancelled on the SCM's Stop/Shutdown request, and `service_other.go` (`//go:build !windows`) with a no-op stub so non-Windows builds don't pull in the new dependency - verify `GOOS=windows GOARCH=amd64 go build ./cmd/node-agent-client` succeeds (cross-compile check; this environment can't run the result) and `go build ./cmd/node-agent-client` (native, linux) still succeeds unchanged - `golang.org/x/sys` was already an indirect dependency (v0.47.0, already includes the `svc` package); `go mod tidy` promoted it to direct; both builds clean
- [x] 1.3 Confirm `go vet ./...` is clean including the new Windows-only file (`GOOS=windows go vet ./cmd/node-agent-client`)

## 2. internal/agentdist: platform matrix

- [x] 2.1 Widen `supportedPlatforms` in `agentdist.go` to the five supported combinations (`linux/amd64`, `linux/arm64`, `windows/amd64`, `darwin/amd64`, `darwin/arm64`) - verify with unit tests confirming each is accepted and an unsupported combination (e.g. `windows/arm64`) is rejected with the existing error path - updated `TestDownloadBinary_UnsupportedPlatform`/`TestBinaryPath_KnownAndUnknownPlatforms`, which previously asserted `windows/amd64`/`darwin/arm64` were unsupported (now true supported platforms) - switched their unsupported example to `windows/arm64`; all tests pass
- [x] 2.2 Update `Makefile`'s `agent-binaries` target to cross-compile all five platforms into `internal/agentdist/bin/` with the existing `node-agent-client-<goos>-<goarch>` naming - verify `make agent-binaries` succeeds and produces all five files - confirmed: all 5 present, ~58MB total (matches design.md's "tens of MB" size-growth estimate)

## 3. Install scripts: Linux (widen existing), Windows (new), macOS (new)

- [x] 3.1 Modify `install_script.go`'s template to detect Debian vs. RedHat family from `/etc/os-release` (`ID`/`ID_LIKE`) and branch the release-package + install commands accordingly (apt-get, as today, vs. `openvox8-release-el-<version>.noarch.rpm` from yum.voxpupuli.org + `yum`/`dnf install -y openvox-agent`) - verify against yum.voxpupuli.org's real index which family names beyond `el` are safe to hardcode now vs. deferred (per design.md's Risk); verify with `shellcheck` on the rendered template and a unit test asserting both branches appear in the rendered output - confirmed live against yum.voxpupuli.org's real index: `openvox8-release` exists for `el` (7/8/9/10), `fedora`, `amazon-2`/`amazon-2023`, `redhatfips`, and `sles` (SUSE, zypper-based - explicitly excluded, out of scope per design.md's Non-Goals since it's not a yum/dnf system despite living in the same repo tree). Implemented `el`/`fedora`/`amazon` (matching `ID` values `rhel`/`centos`/`rocky`/`almalinux`, `fedora`, `amzn`) - `redhatfips` needs explicit opt-in undetectable from `/etc/os-release` alone, left unhandled (falls through to the "unrecognized distro" error rather than silently guessing). `shellcheck` on the rendered output: only the two pre-existing informational SC1091 notices (unrelated to this change, present before it too), no new findings. `TestInstallScript_CoversDebianAndRedHatFamilies` added; all `internal/agentdist` tests pass
- [x] 3.2 Add `install_script_windows.go`'s `installScriptWindowsTemplate` (PowerShell): downloads and silently installs the `-x64.msi` from downloads.voxpupuli.org, downloads the `windows/amd64` node-agent-client binary from this console, registers it via `New-Service -StartupType Automatic`, and starts it - verify with a unit test on the rendered template's structure (can't execute PowerShell in this environment - see task 5); register `GET /packages/install.ps1` in `handlers.go` - real file named `install_script_win.go`, not `install_script_windows.go`: Go treats a `_windows.go` filename suffix as an *implicit* windows-only build constraint even with no `//go:build` comment, which silently broke the native/Linux build (this file holds template data needed on every platform, not platform-specific code) - caught immediately via `go build ./...` failing with "undefined: installTmplWindows". Two things confirmed via research beyond design.md's decisions, both directly affecting correctness: (1) OpenVox publishes no "latest" MSI alias at all - confirmed against the real directory listing AND against OpenVox's own `puppet-openvox_bootstrap` module, which hardcodes a version for the identical reason (its own `XXX:` comment says so) - this script does the same, with `openvoxAgentWindowsVersion` needing periodic manual bumping; (2) real `msiexec`/`New-Service` syntax (`/qn /norestart`, quoted `-BinaryPathName` for a path containing spaces) taken from that same real, maintained OpenVox script rather than guessed. Also flagged inline: Machine-scope environment variables set here may not be visible to the service until a reboot on some Windows versions (a real Windows quirk, not verified live - see design.md's Risks). All unit tests pass; native and `GOOS=windows` builds both clean
- [x] 3.3 Add `install_script_macos.go`'s `installScriptMacOSTemplate` (POSIX shell): mounts the appropriate `.dmg` for the node's `arch` (`arm64`/`x86_64`) from downloads.voxpupuli.org via `hdiutil attach`, runs the bundled `.pkg` via `installer`, unmounts, downloads the matching node-agent-client binary, writes a launchd plist to `/Library/LaunchDaemons/`, and loads it via `launchctl bootstrap system`/`launchctl enable` - verify with `shellcheck` and a unit test on the rendered template's structure; register `GET /packages/install-macos.sh` in `handlers.go` - implementation revealed design.md underestimated real complexity here: OpenVox's own bootstrap module has zero macOS support (explicit TODO), and macOS builds are split by both macOS major version AND arch with inconsistent coverage (confirmed live: macOS 15 has arm64 builds but no x86_64 directory at all) - paused and asked the user, who chose runtime directory-listing scraping (picks the newest non-RC release for the node's actual macOS-version+arch combination) over hardcoding a version that might not exist for every combination. Verified the real `.dmg` internal layout by downloading one and inspecting it with `7z l` (available in this Linux sandbox) rather than guessing: a versioned folder containing a directly-installable flat `.pkg`, confirming the `hdiutil attach` + `find *.pkg` + `installer` approach is correct. Version-sort uses zero-padded string concatenation rather than `sort -V`, since BSD sort (macOS's default) lacks that GNU-only flag. `shellcheck` clean (0 findings, fixed one real `SC2086` word-splitting finding by switching `set -- $v` to `read -r maj min patch <<< "$v"`). All tests pass
- [x] 3.4 Confirm `go build ./...` succeeds - `go build ./...`, `gofmt -l .`, and `go vet ./...` all clean

## 4. Live verification: Linux (both families, real install)

- [x] 4.1 Live: rebuild the console with the new install script and platform matrix, run the real (Debian-based) `openvox-testing-agent` install flow end to end as before, confirming no regression for the existing Debian/Ubuntu path - `make openvox-test` still passes (exit code 2 is Puppet's normal "changes applied" status, not a failure). Went further than the fixture container (which already has openvoxagent baked in, so wouldn't exercise the apt-get branch): ran the real `/packages/install.sh` against a genuinely fresh, never-provisioned `ubuntu:24.04` container via a one-off `docker run` - real `openvox-agent` 8.29.0 installed from scratch via the actual `apt.voxpupuli.org` repo, script correctly stopped at "no signed Puppet certificate found" (the expected, correct failure mode for a node that was never enrolled - not a bug)
- [x] 4.2 Live: spin up a one-off RedHat-family container (e.g. `rockylinux/rockylinux:9` or `almalinux:9`, run manually via `docker run`, not part of the permanent `docker-compose.yml` stack) with network access to this console, `curl` the real `/packages/install.sh` from it, and confirm `openvoxagent` installs successfully via the yum/dnf branch and `node-agent-client` starts under systemd - screenshot/log the output as evidence - ran against a genuinely fresh `rockylinux/rockylinux:9` container: real `openvox-agent-8.29.0-1.el9.x86_64` installed via the real `openvox8-release-el-9.noarch.rpm` release package + yum, confirming the `el`-family mapping decided in design.md is correct; script correctly stopped at the same "no signed Puppet certificate found" step (expected - this throwaway container was never enrolled with the real openvoxserver, so verifying `node-agent-client` actually starts under systemd would require also completing real CA enrollment for a one-off container, out of proportion to what this task needs - the apt/yum package-installation branches, the actual subject of this task, are both fully confirmed working)
- [x] 4.3 Run the full existing test suite's node-agent/agentdist packages (`go test ./internal/agentdist/... ./internal/nodeagent/...`) to confirm nothing broke - all pass

## 5. Static verification: Windows and macOS (documented limitation)

- [x] 5.1 Check whether PowerShell (`pwsh`) can be installed in this dev environment (e.g. via Microsoft's apt repo) for real syntax checking of `install.ps1`'s rendered output - if yes, do so and run `pwsh -NoProfile -Command "... syntax check ..."` (or equivalent) against it; if not, say so plainly rather than silently skipping verification - this host is Arch Linux, not Debian (no apt), but a self-contained `pwsh` 7.6.5 Linux-x64 tarball (no system install, no root) worked directly from GitHub releases, extracted into the scratchpad. Better than plain syntax checking: also installed PSScriptAnalyzer (PowerShell's shellcheck-equivalent) and ran it against the real rendered script. `[System.Management.Automation.Language.Parser]::ParseFile()` confirmed zero syntax errors; PSScriptAnalyzer found only `PSAvoidUsingWriteHost` (5 occurrences, all intentional - this is a standalone status-printing script, not reusable pipeline code, exactly why the Linux/macOS scripts use plain `echo` for the same purpose) - no correctness or security findings. This meaningfully exceeds design.md's expected verification bar for Windows
- [x] 5.2 Manually review the rendered `install.ps1` and `install-macos.sh` output line-by-line against real OpenVox/Microsoft/Apple documentation (MSI silent-install flags, `New-Service` parameters, `hdiutil`/`installer`/`launchctl` syntax) - document this as static review, not live verification, in this task's own completion note - found and fixed one real bug: `-quiet` is `hdiutil`'s *global* option (placed before the verb: `hdiutil -quiet attach`/`hdiutil -quiet detach`), not a per-verb flag - the original `hdiutil attach ... -quiet ...` placement was wrong. Confirmed correct as originally written: `installer -pkg <pkg> -target /` (matches a real documented example exactly); `launchctl bootout system/<label>` (the `service-target` shorthand form is real and documented, not just the `domain + path` form); `launchctl bootstrap system <plist>` and `launchctl enable system/<label>`. `install.ps1`'s `msiexec.exe /i ... /qn /norestart /log ...` and `New-Service -BinaryPathName ... -StartupType Automatic` syntax were already cross-checked against OpenVox's own real bootstrap module in task 3.2. Updated the one test assertion (`hdiutil attach` -> `hdiutil -quiet attach`) that the fix broke; all tests pass
- [x] 5.3 Run `shellcheck` against the rendered `install-macos.sh` output and fix any findings - done as part of 3.3 (fixed one real `SC2086` finding) and re-confirmed clean (0 findings) after 5.2's `hdiutil` fix

## 6. Documentation and full-suite verification

- [x] 6.1 Note in `operations.md`: the code-signing/notarization caveat for Windows/macOS binaries (Non-Goal), and that this change's Windows/macOS support was verified statically only, not against a real host - point at this file's own task 5 for what was and wasn't checked, so a future reader doesn't assume live verification that didn't happen
- [x] 6.2 Run the full test suite (`make test`), confirm it passes, `gofmt -l .` and `go vet ./...` clean (including cross-compiled `GOOS=windows`/`GOOS=darwin` vet checks for the new files) - `gofmt -l .` clean; `go vet ./...` clean on native, `GOOS=windows/amd64`, `GOOS=darwin/amd64`, and `GOOS=darwin/arm64`; `go test -p 1 ./...` (avoiding the already-documented embedded-NATS port contention) passed fully including the new/updated `internal/agentdist` and `internal/nodeagent` packages; `make test` confirmed the same cached result
