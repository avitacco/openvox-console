## 1. Frontend

- [x] 1.1 Add an "Add node" button to `nodes.tmpl`'s filters row, and an empty container for the dialog content - verify `./frontend/build.sh` succeeds - placed next to the "Nodes" heading (in the existing empty slot of the header's `justify-between` row) rather than the filters row below it: matches this project's own established pattern for a page's primary action button (e.g. `node.tmpl`'s "Run now"), and reads better as a page-level action than a filter. No separate dialog-content container needed - the dialog is built and appended to `document.body` on click, matching `confirmDialog`'s own pattern rather than a pre-existing empty div. `./frontend/build.sh` succeeds
- [x] 1.2 In `nodes.js`, wire the button to build and show a `vox-dialog` (a plain info dialog with a single Close action in its footer - no confirm/cancel semantics needed, so this doesn't go through `confirmDialog`) listing each supported platform (Linux, Windows, macOS) with a real `<a href>` link to that platform's install script, resolved against `window.location.origin` (`/packages/install.sh`, `/packages/install.ps1`, `/packages/install-macos.sh`) so the link text is the full, copyable URL - verify `./frontend/build.sh` succeeds
- [x] 1.3 Confirm no `window.confirm`/`window.alert`/`window.prompt` calls were introduced (grep) and that the new dialog closes via its own close control, matching the existing `vox-dialog` pattern's behavior - grep clean (only the pre-existing comment in `app.js` describing `confirmDialog`'s own purpose); the close wiring (`vox-close` listener removing the dialog, Close button calling `dialog.close()`) mirrors `confirmDialog`'s exact pattern, confirmed by code inspection - live confirmation in section 2

## 2. Live verification

- [x] 2.1 Live: click "Add node" on the real running Nodes page and confirm a real `vox-dialog` appears (not a native popup) listing all three platforms, each with the full, correct absolute URL for this console's actual running origin - screenshot as evidence - confirmed against the real running console: dialog shows all three platforms (Linux, Windows, macOS) with correct `http://localhost:8080/packages/...` URLs matching the browser's actual origin - see `evidence/nodes-page-with-add-node-button.png` and `evidence/add-node-dialog.png`
- [x] 2.2 Live: confirm each listed URL actually resolves to the expected script when fetched (matches the already-live-verified `add-multi-platform-agent-install` routes) and that the dialog closes correctly (its close control, and Escape) - `curl`'d all three real URLs from the running console: `install.sh`, `install.ps1`, `install-macos.sh` all returned `200`. Both the Close button and Escape correctly closed the dialog (confirmed via screenshots showing the Nodes page underneath, unchanged, with focus returned to the Add node button after Escape)

## 3. Documentation and full-suite verification

- [x] 3.1 Run the full test suite (`make test`), confirm it passes, `gofmt -l .` and `go vet ./...` clean (no Go changes are expected in this frontend-only change, but this confirms nothing else broke) - `gofmt -l .` and `go vet ./...` both clean; `go test -p 1 ./...` (avoiding the already-documented embedded-NATS port contention) passed fully; `make test` confirmed the same cached result
