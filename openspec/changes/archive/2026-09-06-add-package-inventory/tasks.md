## 1. openvoxdb client

- [x] 1.1 Add a `Package` type (`Certname`, `PackageName`, `Provider`, `Version`) and `NodePackages(ctx, certname) ([]Package, error)` to `internal/openvoxdb` (PQL: `package_inventory { certname = <certname> }`), mirroring `Facts`'s existing shape - verify with a unit test decoding a realistic response body - `TestPackage_Decodes`
- [x] 1.2 Add `SearchPackages(ctx, name string, version *string) ([]Package, error)` to `internal/openvoxdb` (PQL: `package_inventory { package_name = <name> }`, adding `and version = <version>` when version is non-nil) - verify with unit tests covering both the name-only and name+version query shapes - `TestSearchPackages_NameOnly`/`TestSearchPackages_NameAndVersion`, using a `fakeQueryServer` httptest helper to assert the exact generated PQL string for both shapes (no prior pattern in this file tested PQL construction directly, only integration-tested against a live openvoxdb, so this is a new but locally-scoped test helper)
- [x] 1.3 Confirm `go build ./...` succeeds

## 2. Package-inventory API

- [x] 2.1 Create `internal/packageinventory` with a narrow `queryClient` interface (`NodePackages`, `SearchPackages`) that `*openvoxdb.Client` satisfies, and `Handlers` registering `GET /api/v1/nodes/{name}/packages` and `GET /api/v1/packages?name=&version=` under `nodes:read`, per design.md - verify with unit tests using a fake `queryClient`: a node with packages, a node with none (empty list, not an error), a name-only search, a name+version search, and no-match (empty result, not an error) - all 6 tests pass (also added a missing-`name`-param 400 case, since `searchPackages` needed to decide what happens with no query param at all - not explicitly in the spec's scenarios but required to implement the handler)
- [x] 2.2 Wire `packageinventory.NewHandlers(...)` into `cmd/console/main.go` alongside the existing `inventory.NewHandlers(...)` registration - verify `go build ./...` succeeds

## 3. Frontend: node detail page

- [x] 3.1 Add a "Packages" section to `node.html`/`node.js`, listing the node's installed packages (name, version, provider) fetched from `GET /api/v1/nodes/{name}/packages`, rendered the same way the existing Facts section is - verify `./frontend/build.sh` succeeds
- [x] 3.2 Live: view a real node's detail page and confirm the Packages section renders (a real list if this environment has package data by this point, an empty state otherwise - see section 5) - screenshot as evidence - confirmed against the real running console: `openvox-testing-agent`'s detail page renders the new Packages section with the "No package data reported" empty state (matches section 5's finding, not an error) - see `evidence/node-packages-empty-state.png`

## 4. Frontend: fleet-wide packages page

- [x] 4.1 Add `packages.html`/`packages.js`: a name (required) + version (optional) search form and a results table (node name linking to its detail page, version, provider), calling `GET /api/v1/packages?name=&version=` - verify `./frontend/build.sh` succeeds - added `frontend/gen/main.go`'s page registry entry too (not explicitly called out in this task, but required for `packages.html` to actually be generated)
- [x] 4.2 Add a "Packages" nav link alongside the existing Nodes/Groups/Jobs links in the shared layout template - verify `./frontend/build.sh` succeeds - placed directly after Nodes, using the `collection` icon (no dedicated "package" icon exists in voxblocks' icon set) and left unconditionally visible like Nodes/Groups (both also `nodes:read`/`classifier:read`-gated with no nav permission check), rather than adding a new permission-gated nav-link id like Jobs/Deploys/Activity/Admin use
- [x] 4.3 Live: search a real package name (with and without a version) and confirm the results table renders correctly (real matches if available by this point, an empty state otherwise) - screenshot as evidence - confirmed against the real running console: the new Packages page and nav link render correctly (`evidence/packages-page.png`), and searching "openssl" both without (`evidence/search-name-only-empty.png`) and with (`evidence/search-name-and-version-empty.png`) a version correctly shows the "No matching nodes" empty state, not an error

## 5. Live verification of real package-inventory data

- [x] 5.1 Investigate whether this dev environment's `openvoxserver`/`openvox-testing-agent` stack can be made to produce real `package_inventory` rows in openvoxdb (e.g. an agent-side `puppet.conf` setting) - if a workable setting/mechanism exists, apply it and re-run `make openvox-test`; if it doesn't, or if it hits the same PDB command-version gap `operations.md` already documents for `corrective_change`, stop and document the finding instead of forcing it - checked `puppet config print all` against the real `openvox-testing-agent` image (Puppet 8.28.1): no `package_inventory_enabled` setting, or anything else matching `package`/`inventory` beyond the unrelated `cert_inventory` (a CA setting). No agent-side setting exists in this environment to populate `package_inventory` - stopping here per this task's own instruction, and per design.md's Non-Goals (a control-repo module to populate it is explicitly out of scope for this change)
- [x] 5.2 Confirm via direct PQL queries against the real openvoxdb whether `package_inventory {}` now returns real rows for the test node(s) - re-queried `package_inventory {}` directly against the real openvoxdb: still `[]`, consistent with 5.1's finding that nothing in this environment populates it
- [x] 5.3 Live: if 5.2 found real data, repeat 3.2 and 4.3 against it and confirm the node detail Packages section and the fleet-wide search both show real, correct package data (not just the empty state) - screenshot as evidence. If 5.2 confirmed this environment cannot produce real data, this task is superseded by that finding - the empty-state screenshots from 3.2/4.3 already demonstrate the correct (non-error) behavior for that case - superseded: 5.2 confirmed this environment has no package data, and no real-data path was found in 5.1

## 6. Documentation and full-suite verification

- [x] 6.1 Note in `operations.md`, alongside the existing `corrective_change` note, what 5.1/5.2 found - either how real package-inventory data was enabled for this dev environment, or the specific limitation preventing it (matching the existing `corrective_change` write-up's level of detail)
- [x] 6.2 Run the full test suite (`make test`), confirm it passes, `gofmt -l .` and `go vet ./...` clean - `gofmt -l .` and `go vet ./...` both clean; `go test -p 1 ./...` (avoiding the embedded-NATS port contention already documented for prior changes) passed fully including the new `internal/packageinventory` and updated `internal/openvoxdb` packages, then `make test` confirmed the same (cached) result
