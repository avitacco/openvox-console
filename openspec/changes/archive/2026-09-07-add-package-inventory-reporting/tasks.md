## 1. Linux (install.sh): Debian and RedHat package-inventory fact

- [x] 1.1 Add a step to `install_script.go`'s template that writes an executable `/opt/puppetlabs/facter/facts.d/package_inventory.sh` - branching the same way the existing package-manager detection already does: `dpkg-query -W -f='${Package}\t${Version}\n'` (provider `"apt"`) on Debian, `rpm -qa --queryformat '%{NAME}\t%{VERSION}-%{RELEASE}\n'` (provider `"rpm"`) on RedHat - emitting `{"_puppet_inventory_1": {"packages": [[name, version, provider], ...]}}` on stdout - verify with `shellcheck` on the rendered template and a unit test asserting the facts.d file-write step and both branches appear in the rendered output - written as two separate `<<'FACT'` (quoted-delimiter) heredocs, one per branch, so the outer install.sh doesn't prematurely expand the inner fact script's own `$1`/`$2`/`${Package}` variables at write time; `shellcheck` clean (only the two pre-existing informational SC1091 notices); `TestInstallScript_SetsUpPackageInventoryReporting` added
- [x] 1.2 Confirm `go build ./...` succeeds

## 2. Windows (install.ps1): package-inventory fact

- [x] 2.1 Add a step to `install_script_win.go`'s template that writes `C:\ProgramData\PuppetLabs\facter\facts.d\package_inventory.ps1` - using `Get-Package | Select-Object Name, Version, ProviderName` reshaped into the `[name, version, provider]` tuple format and emitted as the `_puppet_inventory_1` JSON shape via `ConvertTo-Json` - verify with a unit test asserting the facts.d file-write step appears in the rendered output (can't execute PowerShell logic end-to-end in this environment - see section 5 for what static verification covers) - written to a literal (`@'...'@`) here-string so install.ps1 itself doesn't try to expand the fact script's own `$_`/`$Packages` variables at install time; `TestInstallScriptWindows_SetsUpPackageInventoryReporting` added
- [x] 2.2 Confirm `go build ./...` succeeds

## 3. macOS (install-macos.sh): package-inventory fact

- [x] 3.1 Add a step to `install_script_macos.go`'s template that writes an executable `/opt/puppetlabs/facter/facts.d/package_inventory.sh` - using `pkgutil --pkgs` plus `pkgutil --pkg-info <id>` per package ID for its version, provider `"pkgutil"` - emitting the same `_puppet_inventory_1` JSON shape - verify with `shellcheck` on the rendered template and a unit test asserting the facts.d file-write step appears in the rendered output - used `while IFS= read -r` (not `for x in $(...)`) to iterate `pkgutil --pkgs`' output and `paste -sd, -` to join entries, avoiding the classic word-splitting anti-pattern entirely; `shellcheck` clean (0 findings); `TestInstallScriptMacOS_SetsUpPackageInventoryReporting` added
- [x] 3.2 Confirm `go build ./...` succeeds

## 4. Live verification: Linux (real package_inventory rows in openvoxdb)

- [x] 4.1 Live: rebuild the console, run the full install flow (per `operations.md`'s now-established pattern: fresh Ubuntu container, enroll, re-run install.sh to completion) against a fresh Debian-family container, then run a real Puppet run (`puppet agent -t`) and confirm via a direct PQL query (`package_inventory { certname = "<the test node>" }`) that real rows now exist in openvoxdb - screenshot/log the query result as evidence - confirmed on a genuinely fresh `ubuntu:24.04` container (certname `ac48233cda5b`): the facts.d script itself produced valid JSON for 129 real installed packages when run directly, and after a real `puppet agent -t` run, `package_inventory { certname = "ac48233cda5b" }` returned real rows (`bsdutils`, `libuuid1`, `libcap2`, `curl`, etc., all `provider: "apt"`) - the first time this project has ever had real package-inventory data in openvoxdb
- [x] 4.2 Live: repeat against a fresh RedHat-family container (e.g. `rockylinux/rockylinux:9`), confirming the `rpm`-provider branch produces real rows too - confirmed on a genuinely fresh `rockylinux/rockylinux:9` container (certname `0284fdf14d71`): real `openvox-agent` installed via the `el`-family yum branch, package-inventory step ran, and after a real `puppet agent -t` run, `package_inventory { certname = "0284fdf14d71" }` returned real `rpm`-provider rows (`libevent`, `keyutils-libs`, `vim-minimal`, etc.)
- [x] 4.3 Live: view the real node detail page and the fleet-wide Packages page for one of these test nodes and confirm real package data now renders (not the empty state `add-package-inventory` verified before) - screenshot as evidence - confirmed against the real running console: the Debian test node's detail page renders its real ~129-package table (`evidence/01-node-packages-real.png`), and the fleet-wide Packages page's search for "curl" correctly returns that node with the real installed version (`evidence/02-packages-search-real.png`) - the first time either page has shown anything but the empty state
- [x] 4.4 Run `go test ./internal/agentdist/...` to confirm nothing broke - all pass; test containers cleaned up

## 5. Static verification: Windows and macOS

- [x] 5.1 Use the `pwsh`/PSScriptAnalyzer setup from `add-multi-platform-agent-install` (self-contained binary in the scratchpad, no system install) to parse and lint the rendered `install.ps1` output, confirming zero syntax errors and reviewing any lint findings - `ParseFile()` confirmed zero syntax errors; PSScriptAnalyzer found only `PSAvoidUsingWriteHost` (now 6 occurrences, one more than before from the new status line) - same intentional pattern already accepted in `add-multi-platform-agent-install`, no correctness findings
- [x] 5.2 Manually review the rendered `install-macos.sh` and `install.ps1` package-inventory steps line-by-line against real Apple/Microsoft documentation (`pkgutil` flags, `Get-Package`/`ConvertTo-Json` parameter names) - document this as static review, not live verification - caught and resolved a real ambiguity: an initial search suggested `pkgutil --pkg-info`'s version field might be named `pkg-version:` rather than `version:` (what the script uses) - confirmed via a real example output (scriptingosx.com) that `version:` is correct for `--pkg-info`/`--info`, and that `pkg-version:` only appears in a *different* subcommand's output (`--file-info`), which is what caused the ambiguity. `Get-Package`'s `Name`/`Version`/`ProviderName` properties confirmed real via Microsoft's own documentation. No code changes needed - both scripts were already correct
- [x] 5.3 Run `shellcheck` against the rendered `install-macos.sh` output (including the new step) and fix any findings - done as part of task 3.1: 0 findings

## 6. Documentation and full-suite verification

- [x] 6.1 Update `operations.md`'s existing "Package inventory: no data source in this dev environment" section to reflect that this is now solved for Linux (real data flowing) and what remains static-only (Windows/macOS) - don't leave the old section reading as still-current if it no longer is - retitled to "Package inventory: solved for Linux, static-only for Windows/macOS", keeping the original investigation as context rather than deleting it
- [x] 6.2 Run the full test suite (`make test`), confirm it passes, `gofmt -l .` and `go vet ./...` clean - `gofmt -l .` and `go vet ./...` both clean; `go test -p 1 ./...` (avoiding the already-documented embedded-NATS port contention) passed fully; `make test` confirmed the same cached result
