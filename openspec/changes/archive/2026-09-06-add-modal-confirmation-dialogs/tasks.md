## 1. Reusable helper

- [x] 1.1 Add `confirmDialog({ heading, body, confirmLabel, danger })` to `frontend/src/app.js` per design.md's implementation, returning a `Promise<boolean>` resolved via a single `vox-close` listener - verify `./frontend/build.sh` succeeds - deviated from design.md's snippet on button variants: `vox-button`'s real `ButtonVariant` type (`frontend/node_modules/@openvoxproject/voxblocks/src/components/button/vox-button.ts`) is `'brand' | 'alt' | 'danger' | 'ghost'`, not `'secondary'/'primary'` as design.md's code block used - those don't match any CSS class on the real component and would render unstyled. Used `alt` for Cancel and `brand` (else `danger`) for Confirm, matching this codebase's actual, near-universal convention (`brand`/`danger`/`ghost` used 11/6/1 times respectively in `frontend/src/*.js` and templates; `primary` appears exactly once, in nodes.js's pre-existing Sign button, itself likely a latent bug, out of this change's scope) - `./frontend/build.sh` succeeds

## 2. Migrate the Nodes page

- [x] 2.1 Replace `nodes.js`'s Revoke `window.confirm()` call with `await confirmDialog(...)`, making its click handler `async`, escaping the certname and infrastructure reason now that they render as real HTML - verify `./frontend/build.sh` succeeds
- [x] 2.2 Replace `nodes.js`'s Clean `window.confirm()` call the same way - verify `./frontend/build.sh` succeeds
- [x] 2.3 Confirm no `window.confirm`/`window.alert`/`window.prompt` calls remain anywhere in `frontend/src/*.js` - verify by grep - only remaining match is `app.js`'s own comment describing `confirmDialog`'s purpose, no actual calls

## 3. Live verification

- [x] 3.1 Live: click Revoke on a real signed certificate and confirm a real `vox-dialog` modal appears (not a native browser popup), showing the correct heading and body text with the certname correctly rendered - screenshot as evidence - verified against the real running console (`openvox-console-test-ubuntu`, a real signed cert) via a Playwright-driven headless Chromium (`chromium-cli` isn't available in this environment; used `playwright-core` against the system `/usr/bin/chromium` instead - see `evidence/` for driver notes) - see `evidence/revoke-dialog.png`: heading "Revoke certificate", body `Revoke the certificate for "openvox-console-test-ubuntu"? It will immediately lose access on its next connection attempt.`, distinct Cancel/Revoke buttons, dimmed backdrop, native `<dialog>`-based modal (not `window.confirm()`)
- [x] 3.2 Live: confirm clicking the dialog's Cancel button, its ✕ button, and pressing Escape all close the dialog without performing the revoke (verify via the API/page state that nothing changed) - all three verified independently against the real console: cert status stayed "signed" and the Revoke button remained present after each (`evidence/after-cancel.png` for the Cancel case; ✕ and Escape produced the identical unchanged-state screenshot, not separately saved)
- [x] 3.3 Live: confirm clicking the dialog's Confirm button actually performs the revoke (state changes as expected) - screenshot the completed state as evidence - confirmed: cert status flipped from "signed" to "revoked" and the Revoke button disappeared (Clean remained, matching `certActionsCell`'s status-gated buttons) - see `evidence/after-revoke-confirmed.png`
- [x] 3.4 Live: repeat 3.1-3.3 for Clean, and for both actions against an infrastructure-flagged certificate (confirming the reason-specific wording still renders correctly, with the certname/reason properly escaped rather than raw HTML-injected) - Clean repeated against the now-revoked `openvox-console-test-ubuntu`: dialog (`evidence/clean-dialog.png`) showed the full re-enroll wording, confirming it (`evidence/after-clean-confirmed.png`) flipped cert status to "unknown" with no action buttons remaining (CA record fully removed). For the infrastructure case, enabled "Show infrastructure certs" and opened both dialogs against the real `node-transport` infrastructure cert - `evidence/infra-revoke-dialog.png` and `evidence/infra-clean-dialog.png` both show the reason-specific wording (`"node-transport" is infrastructure, not a managed node: the certificate this console uses for its node transport's own server identity. Acting on it may break real functionality that depends on it.`), correctly quoted/escaped, in place of the generic copy - deliberately Cancelled both rather than confirming, since actually revoking/cleaning a real infrastructure identity this console depends on (its own node-transport server cert) would break this dev environment, and the wording/escaping is what these two scenarios needed to verify, not the underlying revoke/clean mechanics (already proven against the managed node in 3.1-3.3)

## 4. Documentation and full-suite verification

- [x] 4.1 Run the full test suite (`make test`), confirm it passes, `gofmt -l .` and `go vet ./...` clean (no Go changes are expected in this frontend-only change, but this confirms nothing else broke) - `gofmt -l .` and `go vet ./...` both clean. `go test ./...` (via `-p 1`, to rule out the embedded-NATS port contention `add-dashboard-connectivity-status` already documented - `nodetransport`/`nodeagent`/`orchestrator` tests each spin up their own embedded NATS server and can time out when several run in parallel, reproducing cleanly in isolation every time) passed fully, with no failures traceable to this change's frontend-only diff. Along the way, fixed two pre-existing environment gaps unrelated to this change, discovered because this session's `make up` started from entirely fresh Docker volumes with no prior classifier/deploy state: (1) no node group existed to classify `openvox-testing-agent`, so its ENC response was empty and `internal/openvoxdb`'s `TestEvents` had no real resource event to read - added a group (id 212, "test-fixture-default") pinning that node to the `code_manager_test_marker` class from the control-repo fixture; (2) the deployed `openvox-code/environments/production` symlink pointed to an absolute host path baked in by a prior deploy, which resolves to nothing inside the `openvoxserver` container's own bind-mounted view of that same directory tree, causing `Could not find class` failures once the ENC did return a class - repointed it to an equivalent relative symlink and restarted `openvoxserver` to drop its cached (broken) environment
